batch_size: 64                  # batch size
epochs: 35                      # total number of epochs5
eval_every_n_epochs: 1          # validation frequency
fine_tune_from: pretrain  # sub directory of pre-trained model in ./ckpt
log_every_n_steps: 50           # print training log frequency
init_lr: 0.0005                 # initial learning rate for the prediction head
init_base_lr: 0.0001            # initial learning rate for the base GNN encoder
weight_decay: 1e-7              # weight decay of Adam
gpu: cuda:1                     # training GPU
task_name: Cardiotoxicity5             # inlcuding Cardiotoxicity5、Carcinogenicity、Acute_oral_toxicity、Ames、BCF、CYP2C9、LC50、LC50DM、NR_ER、NR_PPAR_gamma、IGC50、'NR_AR', 'NR_AR_LBD', 'NR_AhR', 'NR_Aromatase', 'NR_ER', 'NR_ER_LBD', 'NR_PPAR_gamma', 'SR_ARE', 'SR_ATAD5', 'SR_HSE', 'SR_MMP', 'SR_p53'


seed: 0
model_num: model.pth
pred_n_layer: 3
model: 
  num_layer: 3                  # number of graph conv layers
  emb_dim: 300                  # embedding dimension in graph conv layers
  feat_dim: 512                 # output feature dimention
  drop_ratio: 0.5               # dropout ratio
  pool: mean                    # readout pooling (i.e., mean/max/add)

dataset:
  num_workers: 8               # dataloader number of workers
  valid_size: 0.1               # ratio of validation data
  test_size: 0.1                # ratio of test data